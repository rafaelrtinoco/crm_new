# Claude Code — local settings (preferências pessoais não devem ir pro git)
.claude/settings.local.json

# Logs gerados pelos hooks (ative se for usar)
.claude/logs/
.claude/bash-commands.log

# Cache local
.claude/.cache/

aula-wsl-claude-code-starter.md

# Node / build
node_modules
dist
dist-ssr
*.local
coverage
*.tsbuildinfo

# Env (exceto .env.example, sem valores)
.env
.env.*
!.env.example

# Supabase local
supabase/.branches
supabase/.temp

